def perform_operation(multiplier, multiplicand):
    result = 0
    for _ in range(multiplier):
    # result = multiplication.perform_operation(result, multiplicand)
      return multiplier * multiplicand